# 검수 보고서 — G1·B1 크로스 배치 (B0 인프라 → DEC-015 iOS 검증)

- 검수일: 2026-07-24 (첫 검수 — reviews/ 신설)
- 검수 범위(커밋 기준): `7ca834f`(B0 인프라) · `7509caf`(G1 웹 파일럿) · `58f57b5`(G1 iOS 슬라이스)
  · `f411459`(G2-B1 core 12종) · `b9f0538`(08-mcp 스펙, 문서만) · `f456624`(DEC-015 iOS 검증)
- 검수 기준: 03-web-arch / 04-ios-arch(DEC-010) / DECISIONS.md / ledger 정직성 / 배치 간 드리프트
- 방법: 스펙 전문 대조 + 전 소스 정독 + **전 게이트 재실행 실측**(아래 §1)
- 미커밋 인플라이트 작업(B2 layout·finance-data·CI v3 레인·parity 인프라)은 §5 조기 경보로만
  다루고 다음 검수의 본대상으로 이월한다. 스냅샷 시각 기준이며 검수 중에도 트리가 움직였다.

## 총평: **통과** (P0 없음)

원장 정직성 항목 전수 실측에서 "done인데 미완" **0건**. 웹·iOS·토큰·사이즈·벤치의 원장 주장
전부가 검수자 독립 재실행으로 재현됐다. 규약 위반은 P1 2건·P2 6건 — 모두 방향 수정이 아니라
다음 배치 전 정비 성격이다.

---

## 1. 원장 정직성 — 전수 실측 결과

| 원장 주장                                                         | 실측                                                                                                                                                                           | 판정    |
| ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------- |
| 웹 tests done (파일럿 3 + B1 12행)                                | `vitest run` 12파일 **137/137 통과** (`.vite` 캐시 삭제 후 — DEC-014-8 프로토콜 준수)                                                                                          | ✅ 일치 |
| size-baseline 15엔트리                                            | `npm run size:web` PASS — 코어 5.08KB/8KB, 개별·평균·p95 전부 예산 내, baseline 값과 일치                                                                                      | ✅ 일치 |
| tokens (B0)                                                       | `npm run tokens:test` 15/15 통과                                                                                                                                               | ✅ 일치 |
| Modal bench "open p95 12.2ms / close 8.7ms / longtask 0 / 누수 0" | `benchmarks/results/modal-s3.json` 실재, 수치 4개 전부 원장 문구와 일치                                                                                                        | ✅ 일치 |
| iOS "빌드·XCTest 검증 완료(DEC-015)"                              | **검수자 독립 재현**: DEC-015-3 우회 경로로 simctl xctest 재실행 → **31/31 통과** (iPhone 17 / iOS 26.2). 스위트 구성(JdLayout 12·Core 9·UIKit 6·SwiftUI 4)도 소스 실측과 일치 | ✅ 일치 |
| iOS 커밋 시점 "unverified" 정직 기록 → DEC-015로 notes 갱신       | 커밋 58f57b5 원장 notes에 미검증 명기 → f456624에서 검증 완료로 갱신. 시점별 상태가 전부 참                                                                                    | ✅ 모범 |

부속 확인: `xcodebuild` 손상 실측(libclang 서명 invalid)도 검수자 환경에서 동일 재현 —
DEC-015-2의 기술 서술이 정확하다. 단 §4-B의 false-green 주의 참조.

---

## 2. 규약 준수 — 웹 (03-web-arch)

**통과 확인** (전 컴포넌트 15폴더 실측):

- element/index 2파일 분리 15/15, index.ts 4줄 정형 패턴 균일. element.ts 부작용 0.
- SSR 4규칙: 톱레벨 DOM 사용 grep 0건(§3.1-1 스텁 베이스 포함), constructor DOM 접근 0건,
  `Math.random`/`Date.now` 0건(jd-uid 증분 카운터), CSSStyleSheet 지연 생성(styles.ts·style-props RESP_STYLES).
- 이벤트: `jd-input`/`jd-change`/`jd-open`/`jd-close` 사후형 cancelable=false,
  `jd-request-close`만 cancelable — WEB-04 완전 준수. emit() 단일 진입점, composed:false 고정.
- 스타일 계층: 전 컴포넌트 CSS `@layer junds.components` 안, `!important` 0건,
  reflect 속성 셀렉터 + 기본값은 base 클래스(DEC-012-2) 일관.
- 입양 규칙(§3.3): button/text-field/modal/heading/text/page/section 전부 `:scope` 조회 후 입양.

### [P1-1] exports 맵·define.ts ALL 배열 수기 관리 — §6.2 명시 금지 위반

- [packages/web/package.json](../../packages/web/package.json) exports 34엔트리,
  [packages/web/src/define.ts:26](../../packages/web/src/define.ts#L26) `ALL` 17클래스 — 전부 수기.
- 03 §6.2: "**exports 맵 수기 관리 금지** — scripts/gen-exports.mjs가 생성하고 CI에서 drift 검사".
  생성기·drift 검사 모두 부재(레포 전수 grep 확인). define.ts 주석 스스로 "배치마다 여기(와
  exports)에 추가"라고 수기 확장을 예고한다.
- 위험: B2부터 배치당 10±종씩 3곳(components/·exports·ALL)을 손으로 동기화 — 누락 1건이
  소비자 import 실패 또는 defineJunds 전량 등록 누락으로 나타나며, 테스트가 index.js 직접
  import라서 **누락을 잡지 못한다**. 390종 스케일 전 지금이 생성기 도입 최저 비용 시점.

### [P2-1] 토큰 우회 색 리터럴 — "토큰만 사용" 원칙의 국소 이탈

- [button.css.ts:82](../../packages/web/src/components/button/button.css.ts#L82)
  `rgba(220,63,63,.25)` — `--jd-color-danger`(#dc3f3f)의 25% 파생값을 하드코딩.
  **같은 파일 :86-88이 이미 `color-mix(in srgb, var(--jd-color-danger) 40%, transparent)`
  관용구를 쓴다** — 소비자가 danger 토큰을 오버라이드하면 호버 글로우만 낡은 빨강으로 남는다.
  `color-mix(… 25%, transparent)`로 교체 권고.
- [button.css.ts:29](../../packages/web/src/components/button/button.css.ts#L29)·[:78](../../packages/web/src/components/button/button.css.ts#L78)
  `color: #fff` — on-primary/on-accent 토큰 부재의 귀결. iOS는 동일 간극을 DEC-013-7로
  기록(`jdWhite` 파일 내 상수 + G2 재심의)했는데 **웹은 미기록** — 플랫폼 간 기록 규율 비대칭.
- [modal.css.ts:19](../../packages/web/src/components/modal/modal.css.ts#L19) 백드롭
  `rgba(0,0,0,.3)` — 오버레이 토큰 부재(v2 black/30 패리티). 동일하게 G2 재심의 목록에 올릴 것.
- button/modal의 중립 그림자 리터럴(`0 1px 2px rgba(0,0,0,.1)` 등)은 다크 모드에서
  `--jd-shadow-*` 램프(DEC-008-3 승격분)가 오버라이드되는 것과 달리 라이트 값으로 고정된다 —
  v2 패리티 논거는 성립하나, 다크 그림자 승격 결정과의 정합은 G2 그림자 어휘 재심의에 포함 권고.

### [P2-2] radius 16px의 배치 간 번역 불일치

- G1 [modal.css.ts:29](../../packages/web/src/components/modal/modal.css.ts#L29): v2 radius
  `2xl`(16px) → `var(--jd-radius-2xl)`(1rem) — **토큰 참조**.
- B1 [section.css.ts:13](../../packages/web/src/components/section/section.css.ts#L13): v2
  styleProps radius `xl`(16px) → **리터럴 `16px`**.
- 같은 목표값 16px를 배치마다 다르게 번역했다. B1의 선택은 DEC-014-1(어휘 충돌 시 v2 리터럴)의
  연장으로 방어 가능하나, 이름-값이 아닌 **값 기준 별칭**(16px ⇒ --jd-radius-2xl)이 성립하는
  경우의 규칙이 없다. G2 radius 어휘 통합 재심의에 이 사례를 인풋으로 등재할 것.

---

## 3. 규약 준수 — iOS (04-ios-arch · DEC-010)

**통과 확인**:

- **계층 독립 grep 게이트(수동 실행): 위반 0건** — JunDSSwiftUI에 `import JunDSUIKit` 없음,
  JunDSUIKit에 `import JunDSSwiftUI` 없음. JdSwiftUIFont의 시스템 UIKit 직접 import는
  DEC-010·04 §4.2 명시 허용 경로이고 파일 주석으로 근거를 남겼다.
- Core 순수성: JdToken.swift(생성물)의 UIKit/SwiftUI import는 DEC-013-2 기록 그대로.
  신규 Core 파일(JdOptions/JdMotion/Spec 2종)은 Foundation+CoreGraphics만 — A2 준수.
- 명명 A8: `JdButton`(SwiftUI) / `JdButtonView`(UIKit) / 옵션 enum Core 1회 — 전 파일 일치.
  left/right 앵커 미제공(§5.6) 확인 — DSL 표면에 존재하지 않는다.
- JdLayout: diff 저장소·constant 동일 객체 갱신·자동 identifier(`jd @file:line`)·
  superview 부재 precondition — §5.4 규범 그대로. **DEC-013-3(동일 파일 발원 한정 diff)**
  구현·회귀 테스트(`test_diff_scope_does_not_clobber_other_file_constraints`) 모두 실재.
- 접근성: 고정 height 0건(전부 GTE minHeight — §7.2), `adjustsFontForContentSizeCategory`
  전 라벨 적용, 스케일 폰트 UIFontMetrics 경유, 에러 라이브 알림(UIAccessibility.announcement).
- 횡단 의미론(DEC-012·013 번역표): variant 4종 rawValue = 웹 문자열(테스트로 고정),
  size 램프 32/40/48 = DEC-013-4 기록과 일치, persistent=interactiveDismissDisabled,
  스와이프다운→`.backdrop`, jd-input/jd-change ↔ onTextChange/onCommit, ghost 눌림
  0x6B68_8029 = 웹 color-mix(muted 16%) 등가. 모순 0건.

### [P1-2] DEC-010 계층 독립 게이트가 CI에 없다

- 04 §2.2: "계층 독립 게이트 (DEC-010): …를 **CI grep으로 금지**한다(Core의 UIKit 금지
  게이트와 동일 방식)". 현행 CI 어디에도 이 grep 잡이 없고, 인플라이트
  [junds-v3.yml](../../.github/workflows/junds-v3.yml)의 11게이트 설계에도 **누락**돼 있다.
- DEC-010은 이 프로젝트에서 사람이 직접 번복·확정한 최상위 결정이다. 그 유일한 자동 강제
  장치가 없는 채로 S90→M187 배치가 가속되면, Representable 지름길 유혹이 가장 큰 L급
  구현기에 무방비가 된다. 잡 하나(grep 2줄 + Generated/ 제외 Core 검사)면 된다 —
  junds-v3.yml 합류 전 추가 권고.

### [P2-3] 폰트 브리지 판정 로직이 양 계층에 중복 — Core 이전 극대화 원칙과 마찰

- [JdFontBridge.swift:6-20](../../packages/ios/Sources/JunDSUIKit/Bridge/JdFontBridge.swift#L6)과
  [JdSwiftUIFont.swift:8-29](../../packages/ios/Sources/JunDSSwiftUI/Bridge/JdSwiftUIFont.swift#L8)에
  weight 임계값(700/600/500)·textStyle 판정(12/13/15/17)이 **동일 로직으로 2회** 존재한다.
- §4.2 판정 규칙 3("측정·판정은 Core의 순수 함수") 기준, `size→중립 role`·`weight→중립 단계`
  판정은 Core로 내릴 수 있다(04 §6 `JdTextRole` 스케치가 원래 이 답이었고, 생성물이 role을
  방출하지 않아 생긴 공백 — DEC-013-2의 연장). 지금은 파일럿 2벌이라 무해하지만 S/M 양산에서
  이 브리지가 사실상의 타이포 정본이 된다 — 토큰 생성기 개정(A2 재심의) 때 함께 해소할 것.

### [P2-4] iOS 테스트 2건이 이름만큼 검증하지 않음 (어서션 공허)

- [JdLayoutTests.swift:204](../../packages/ios/Tests/JunDSUIKitTests/JdLayoutTests.swift#L204)
  `test_error_state_toggles_row` — 에러 행 토글을 주장하나 `errorStack.isHidden` 어서션이
  없고 text 왕복만 검증한다.
- [JdLayoutTests.swift:213](../../packages/ios/Tests/JunDSUIKitTests/JdLayoutTests.swift#L213)
  `test_min_height_follows_size` — `subviews.first != nil`만 확인. 사이즈 추종 미검증
  (`XCTAssertJdConstraint(field, .height, 48)` 한 줄이면 된다 — 내부 field 접근 필요).
- 31/31 "통과"가 참이어도 이 2건의 통과는 정보량이 거의 0이다. 파일럿은 이후 배치의 복제
  원본이므로(§4.1 정본 패턴), 공허 어서션이 템플릿으로 전파되기 전에 보강 권고.

### [P2-5] demo/README.md의 결정 참조 오류

- [demo/README.md:11](../../demo/README.md#L11) "코드서명 손상 상태**(DEC-014)**" —
  DEC-014는 B1 웹 core 배치 결정이다. 올바른 참조는 **DEC-015**(-2). append-only 결정
  대장의 가치는 참조 무결성이다 — 1자 수정이니 다음 커밋에 편승 권고.

---

## 4. 기타 관찰 (심각도 없음)

- **A. 드리프트 — 테스트 밀도**: 파일럿 ~12테스트/컴포넌트 vs B1 컨테이너 1±/컴포넌트는
  희석이 아니라 로직 비례다 — B1 로직은 style-props에 집중돼 있고 그 26테스트 + 컨테이너
  공통 불변식 7테스트가 실질을 커버한다. 다만 이 논거는 "로직 없는 컴포넌트"에만 성립한다 —
  B2 이후 상태 있는 컴포넌트가 컨테이너식 스모크만 받는지 계속 감시한다.
- **B. `ios:build` false-green**: 이 머신에서 xcodebuild가 libclang 로드 실패를 찍고도
  **exit 0**으로 끝난다(검수 중 실측). CI macos 러너에는 무관하나, 로컬에서 "빌드 통과"
  판단의 근거로 `npm run ios:build`의 종료코드를 쓰지 말 것 — DEC-015-3 우회 경로가 정본.
- **C. 08-mcp 스펙**: "승인 대기" 헤더와 커밋 메시지("게이트 대기")가 일치하고 구현 착수
  흔적 없음 — 게이트 규율 준수 확인.
- **D. 모달 ESC 다중 인스턴스**: 열린 모달 2개에서 ESC 1회가 둘 다 닫는다(문서 레벨 리스너
  스택 부재). G1 스코프 밖(중첩 모달 미지원)이므로 기록만 — Drawer/CommandPalette 합류 시
  오버레이 스택 설계가 필요해진다.

## 5. 인플라이트 조기 경보 (미커밋 — 다음 검수 본대상)

스냅샷(검수 종료 시점 `git status`): B2 layout 10종 + finance-data 전체 + junds-v3.yml +
docs-spec/parity/ + icons/. 검수 판정은 하지 않고 커밋 전 정비 항목만 남긴다:

1. **junds-v3.yml에 DEC-010 grep 게이트 추가** (P1-2 — 합류 전이 최저 비용).
2. [packages/finance-data/src/index.ts](../../packages/finance-data/src/index.ts) 배럴 주석의
   "DEC-014" 인용이 현행 DEC-014와 불일치 — 해당 결정(서버 전용 모듈 배럴 제외)을 DECISIONS에
   실제로 등재하고 번호를 맞출 것 (P2-5와 같은 유형의 참조 무결성).
3. finance-data 테스트가 4/12 모듈에서 성장 중 — 커밋 시점에 stream/livePrices/liveIndices
   (상태·폴링 로직 밀집부) 커버 여부를 다음 검수가 본다.

## 6. 칭찬할 패턴 — DEC-015: 막힌 게이트를 우회로 뚫고 원장을 소급 정정한 수순

이번 검수에서 가장 반복할 가치가 있는 패턴은 iOS 검증 트랙의 수순이다:
**(1)** 커밋 시점에 검증 불가를 원장 notes와 커밋 메시지에 정직하게 명기("빌드 미검증") →
**(2)** 툴체인 사망을 표면 증상이 아니라 원인 수준까지 실측(libclang 서명 invalid, AMFI
SIGKILL, Rosetta 경로까지 배제) → **(3)** CLT 크로스 빌드 + simctl xctest 주입이라는 재현
가능한 우회를 확립하고 명령 전문을 README에 기록 → **(4)** 검증 완료 후 원장 3행 notes를
소급 갱신하고 DEC-015로 결정 대장에 남김. 검수자가 그 README 명령만으로 31/31을 독립
재현했다는 사실이 이 수순의 품질 증명이다. "미검증 done"을 만들지 않고 검증 부채를
추적-상환한 전 과정이 원장 운영의 정본 사례다. (부가: web-a11y-audit.mjs의 "산출물
부재·페이지 0건이면 빈 통과 대신 실패" 가드도 같은 정신이다.)

---

_검수자 주: 본 보고서의 모든 실측은 2026-07-24 00:01~00:12 KST 사이 워크트리
`~/develop/jjunhaa/JunDS-v3`에서 수행. 검수 중 병렬 트랙 커밋(f411459·f456624)이
진행돼, 본문 판정은 각 항목에 명시된 커밋/스냅샷 기준이다._
