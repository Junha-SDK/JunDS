/**
 * jd-global-kis-seeder CSS — 화면에 없는 헤드리스 요소. 흐름·레이아웃에 영향 0.
 */
import { css } from "../../core/styles.js";

export default css`
  @layer junds.components {
    jd-global-kis-seeder {
      display: none;
    }
  }
`;
