/* AUTO-GENERATED by scripts/gen-adapters.mjs — DO NOT EDIT (DEC-044) */
"use client";

/** <jd-live-market-stats> 의 React 어댑터. 표면은 @junds/web 런타임 static props 에서 왔다. */
import "@junds/web/live-market-stats"; // <jd-live-market-stats> 정의 등록 (선등록 승리 가드, §2)
import { createJdElement, type JdBaseProps } from "../internal/createJdElement.js";

export interface LiveMarketStatsProps extends JdBaseProps {
  downCount?: number;
  flatCount?: number;
  kosdaqAmount?: number;
  kospiAmount?: number;
  nxtHref?: string;
  sentiment?: number;
  total?: number;
  totalAmount?: number;
  upCount?: number;
  weightedAvg?: number;
}

export const LiveMarketStats = createJdElement<LiveMarketStatsProps>(
  { tag: "jd-live-market-stats", props: { downCount: "number", flatCount: "number", kosdaqAmount: "number", kospiAmount: "number", nxtHref: "string", sentiment: "number", total: "number", totalAmount: "number", upCount: "number", weightedAvg: "number" }, events: {  } },
  "LiveMarketStats",
);
