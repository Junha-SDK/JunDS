/* AUTO-GENERATED by scripts/gen-adapters.mjs — DO NOT EDIT (DEC-044) */
"use client";

/** <jd-live-ticker> 의 React 어댑터. 표면은 @junds/web 런타임 static props 에서 왔다. */
import "@junds/web/live-ticker"; // <jd-live-ticker> 정의 등록 (선등록 승리 가드, §2)
import { createJdElement, type JdBaseProps } from "../internal/createJdElement.js";

export interface LiveTickerProps extends JdBaseProps {
  label?: string;
  live?: boolean;
  source?: string;
  speed?: number;
  stocks?: unknown;
}

export const LiveTicker = createJdElement<LiveTickerProps>(
  { tag: "jd-live-ticker", props: { label: "string", live: "boolean", source: "string", speed: "number", stocks: "data" }, events: {  } },
  "LiveTicker",
);
