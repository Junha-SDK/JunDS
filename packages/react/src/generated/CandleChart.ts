/* AUTO-GENERATED by scripts/gen-adapters.mjs — DO NOT EDIT (DEC-044) */
"use client";

/** <jd-candle-chart> 의 React 어댑터. 표면은 @junds/web 런타임 static props 에서 왔다. */
import "@junds/web/candle-chart"; // <jd-candle-chart> 정의 등록 (선등록 승리 가드, §2)
import { createJdElement, type JdBaseProps } from "../internal/createJdElement.js";

export interface CandleChartProps extends JdBaseProps {
  candles?: unknown;
  chartType?: string;
  compareLine?: unknown;
  events?: unknown;
  height?: number;
  indicators?: unknown;
  label?: string;
  logScale?: boolean;
  markers?: unknown;
  movingAverages?: unknown;
  noCurrent?: boolean;
  noVolume?: boolean;
  separatorIndex?: number;
  width?: number;
  xLabels?: unknown;
}

export const CandleChart = createJdElement<CandleChartProps>(
  { tag: "jd-candle-chart", props: { candles: "data", chartType: "string", compareLine: "data", events: "data", height: "number", indicators: "data", label: "string", logScale: "boolean", markers: "data", movingAverages: "data", noCurrent: "boolean", noVolume: "boolean", separatorIndex: "number", width: "number", xLabels: "data" }, events: {  } },
  "CandleChart",
);
