/* AUTO-GENERATED by scripts/gen-adapters.mjs — DO NOT EDIT (DEC-044) */
"use client";

/** <jd-chat-thread> 의 React 어댑터. 표면은 @junds/web 공개 element 선언에서 왔다. */
import "@junds/web/chat-thread"; // <jd-chat-thread> 정의 등록 (선등록 승리 가드, §2)
import type { JdChatThread as ChatThreadElement } from "@junds/web/chat-thread/element";
import { createJdElement, type JdBaseProps } from "../internal/createJdElement.js";

interface ChatThreadOwnProps {
  currentUserId?: ChatThreadElement["currentUserId"];
  messages?: ChatThreadElement["messages"];
  noAutoScroll?: ChatThreadElement["noAutoScroll"];
  typingUsers?: ChatThreadElement["typingUsers"];
  onJdMessageClick?: (event: CustomEvent<{ id: string; }>) => void;
  onJdRetry?: (event: CustomEvent<{ id: string; }>) => void;
}

export type ChatThreadProps = JdBaseProps<ChatThreadElement, keyof ChatThreadOwnProps> & ChatThreadOwnProps;

export const ChatThread = createJdElement<ChatThreadElement, ChatThreadProps>(
  { tag: "jd-chat-thread", props: { currentUserId: "string", messages: "data", noAutoScroll: "boolean", typingUsers: "data" }, defaults: { currentUserId: "", noAutoScroll: false }, events: { onJdMessageClick: "jd-message-click", onJdRetry: "jd-retry" } },
  "ChatThread",
);
