/* AUTO-GENERATED by scripts/gen-adapters.mjs — DO NOT EDIT (DEC-044) */
"use client";

/** <jd-reading-goal> 의 React 어댑터. 표면은 @junds/web 런타임 static props 에서 왔다. */
import "@junds/web/reading-goal"; // <jd-reading-goal> 정의 등록 (선등록 승리 가드, §2)
import { createJdElement, type JdBaseProps } from "../internal/createJdElement.js";

export interface ReadingGoalProps extends JdBaseProps {
  current?: number;
  label?: string;
  size?: number;
  target?: number;
  thickness?: number;
  unit?: string;
}

export const ReadingGoal = createJdElement<ReadingGoalProps>(
  { tag: "jd-reading-goal", props: { current: "number", label: "string", size: "number", target: "number", thickness: "number", unit: "string" }, events: {  } },
  "ReadingGoal",
);
