/* AUTO-GENERATED by scripts/gen-adapters.mjs — DO NOT EDIT (DEC-044) */
"use client";

/** <jd-reading-stats> 의 React 어댑터. 표면은 @junds/web 런타임 static props 에서 왔다. */
import "@junds/web/reading-stats"; // <jd-reading-stats> 정의 등록 (선등록 승리 가드, §2)
import { createJdElement, type JdBaseProps } from "../internal/createJdElement.js";

export interface ReadingStatsProps extends JdBaseProps {
  booksCompleted?: number;
  pagesGoal?: number;
  pagesToday?: number;
  streakDays?: number;
  totalMinutes?: number;
}

export const ReadingStats = createJdElement<ReadingStatsProps>(
  { tag: "jd-reading-stats", props: { booksCompleted: "number", pagesGoal: "number", pagesToday: "number", streakDays: "number", totalMinutes: "number" }, events: {  } },
  "ReadingStats",
);
